<h2>
    <%= link_to 'Add New',new_todo_path, class: 'header-menu' %>
    My Todos
</h2>
<ul>
    <% @todos.each do |todo| %>
    <li>
    <%= link_to todo.title, todo_path(todo) %>
    <%= link_to '[Edit]', edit_todo_path(todo), class: 'command' %>
    </li>
    <% end %>
</ul>